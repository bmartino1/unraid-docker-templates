<?xml version="1.0"?>
<Container version="2">
  <Name>cs-unifi-bouncer</Name>
  <Repository>ghcr.io/teifun2/cs-unifi-bouncer</Repository>
  <Registry>https://github.com/teifun2/cs-unifi-bouncer/pkgs/container/cs-unifi-bouncer</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>

  <Support>https://github.com/teifun2/cs-unifi-bouncer/issues</Support>
  <Project>https://github.com/teifun2/cs-unifi-bouncer</Project>
  <ReadMe>https://github.com/teifun2/cs-unifi-bouncer</ReadMe>

  <Overview>This container implements a CrowdSec bouncer for UniFi routers. It blocks malicious IP addresses by dynamically updating UniFi firewall address groups through the UniFi API.&#13;
&#13;
Procedure:&#13;
1. Generate a CrowdSec bouncer API key:&#13;
   cscli bouncers add unifi-bouncer&#13;
&#13;
2. Copy the generated key (it will only be shown once).&#13;
&#13;
3. Set the key as the environment variable CROWDSEC_BOUNCER_API_KEY.&#13;
&#13;
4. Start the container. It will automatically communicate with your UniFi controller and apply firewall rules.&#13;
&#13;
Additional configuration variables can be found here:&#13;
https://github.com/teifun2/cs-unifi-bouncer/pkgs/container/cs-unifi-bouncer#configuration
</Overview>

  <Category>Security Network</Category>

  <WebUI/>

  <TemplateURL/>

  <Icon>https://github.com/teifun2/cs-unifi-bouncer/raw/main/docs/assets/crowdsec_unifi_logo.png</Icon>

  <ExtraParams/>
  <PostArgs/>
  <CPUset/>

  <DateInstalled>1767923169</DateInstalled>

  <DonateText/>
  <DonateLink/>

  <Requires>You must have both a UniFi controller and a CrowdSec instance running. The container must have network access to both services.</Requires>

  <Config Name="CROWDSEC_BOUNCER_API_KEY" Target="CROWDSEC_BOUNCER_API_KEY" Default="" Mode="" Description="CrowdSec bouncer API key used to authenticate with the CrowdSec local API." Type="Variable" Display="always" Required="true" Mask="true"/>
  <Config Name="CROWDSEC_URL" Target="CROWDSEC_URL" Default="http://crowdsec:8080/" Mode="" Description="URL of the CrowdSec local API." Type="Variable" Display="always" Required="true" Mask="false">http://crowdsec:8080/</Config>
  <Config Name="UNIFI_HOST" Target="UNIFI_HOST" Default="" Mode="" Description="Hostname or IP address of the UniFi controller." Type="Variable" Display="always" Required="true" Mask="false"/>
  <Config Name="UNIFI_API_KEY" Target="UNIFI_API_KEY" Default="" Mode="" Description="UniFi API key used for controller authentication." Type="Variable" Display="always" Required="true" Mask="true"/>
  <Config Name="UNIFI_USER" Target="UNIFI_USER" Default="unifi" Mode="" Description="UniFi controller username." Type="Variable" Display="always" Required="true" Mask="false">unifi</Config>
  <Config Name="UNIFI_PASS" Target="UNIFI_PASS" Default="unifi" Mode="" Description="UniFi controller password." Type="Variable" Display="always" Required="true" Mask="true"/>
  <Config Name="UNIFI_IPV6" Target="UNIFI_IPV6" Default="false" Mode="" Description="Enable IPv6 support." Type="Variable" Display="always" Required="false" Mask="false">false</Config>

  <TailscaleStateDir/>
</Container>
